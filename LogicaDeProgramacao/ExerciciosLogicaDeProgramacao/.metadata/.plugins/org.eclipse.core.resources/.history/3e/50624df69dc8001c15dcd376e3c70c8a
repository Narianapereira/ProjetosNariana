package Lista2;

import javax.swing.JOptionPane;

public class L2Ex7 {

	public static void main(String[] args) {

		double[][] notas = new double[4][3];
		double maiorNota = 0;
		String disciplinaMaiorNota = "matemática";
		int disciplinaMedia = 1;
		double somaMedia = 0;
		double media = 0;
		int c = 0;
		int l = 0;
		int opcoesMenu = 0;
		int opcaoBimestre = 0;

		for (l = 0; l < 4; l++) {
			for (c = 0; c < 3; c++) {
				notas[l][c] = Double.parseDouble(JOptionPane.showInputDialog(
						"Digite o valor da nota da " + (l + 1) + "ª linha na " + (c + 1) + " coluna."));
				if (notas[l][c] > maiorNota) {
					maiorNota = notas[l][c];
					if (c == 1) {
						disciplinaMaiorNota = "matemática";
					} else if (c == 2) {
						disciplinaMaiorNota = "física";
					} else {
						disciplinaMaiorNota = "química";
					}
				}
			}
		}
		opcoesMenu = Integer.parseInt(JOptionPane.showInputDialog(
				"Qual opção você deseja acessar? \n 1 - Todas as notas, \n 2 - Maior nota, \n 3 - Média das notas de uma disciplina, \n 4 - Notas de um dos bimestres, \n 5- Encerrar"));

		switch (opcoesMenu) {
		case 1:
			for (l = 0; l < 4; l++) {
				for (c = 0; c < 3; c++) {
					System.out.print(notas[l][c] + " "); // imprime caracter a caracter
				}
				System.out.println(" "); // muda de linha
			}
			break;
		case 2:
			JOptionPane.showMessageDialog(null, "A maior nota foi " + maiorNota + " de " + disciplinaMaiorNota + ".");
			break;
		case 3:
			disciplinaMedia = Integer.parseInt(JOptionPane.showInputDialog(
					"Qual matéria você deseja ver a média? \n 1 - Matemática, \n 2 - Física, \n 3 - Química"));
			// soma das linhas da coluna escolhida através da variável disciplinaMedia
			for (l = 0; l < 4; l++) {
				somaMedia = somaMedia + notas[l][(disciplinaMedia - 1)];
			}
// calculo da media com a soma das linhas							
			media = somaMedia / 4;
			JOptionPane.showMessageDialog(null, "A média desta disciplina foi de " + media);

			break;
		case 4:
			opcaoBimestre = Integer.parseInt(JOptionPane.showInputDialog("Qual bimestre você deseja ver as notas?"));
			if (opcaoBimestre == 1) {
				for (l = 0; l < 2; l++) {
					for (c = 0; c < 3; c++) {
						System.out.print(notas[l][c] + " "); // imprime caracter a caracter
					}
					System.out.println(" "); // muda de linha
				}

			} else {
				for (l = 0; l > 2; l++) {
					for (c = 0; c < 3; c++) {
						System.out.print(notas[l][c] + " "); // imprime caracter a caracter
					}
					System.out.println(" "); // muda de linha
				}
			}
		}
	}
}
